# 2026-09-19 — 사이트 구축과 공개

## 한 일

빈 상태에서 <https://yjinyoo.github.io> 공개까지. al-folio 템플릿을 받아 데모를 걷어내고
(113 MB → 500 KB), 논문 목록·활동 그래프·CV PDF 를 전부 생성물로 만들었다.

## 만든 것

- **논문 45편.** OpenAlex 가 동명이인 7명(습지·유변학·양잠·지진파·1985 크로마토그래피 등)을
  한 저자 레코드에 합쳐 놓은 것을 공저자망 + MIT 소속으로 갈랐다. 표지논문 중복 11쌍 병합.
  DOI 45개 전부 Crossref 조회 확인. CV 에만 있던 공동 1저자 14편과 색인 안 된 저널 2편
  (Soft Science, Micromachines) 을 스크립트 예외로 추가.
- **활동 그래프.** 서드파티(ghchart)는 최근 1년 고정이라 연 단위 지정이 안 돼서 직접 그린다.
  1월부터 3월 10일까지 빈 구간은 자동으로 잘라 3월부터 시작. 매일 갱신 워크플로.
- **CV PDF.** Word COM 이 두 번 멈춰서 PyMuPDF 로 기존 PDF 에서 전화번호를 실제 삭제.
  저장 후 재추출해 남아 있으면 출력을 지우고 실패하게 함.
- **링크 카드(og:image)** 1200×630, LinkedIn Featured 카드에 들어감.
- **Person 구조화 데이터.** 이름 변형 4종(유영진 포함), 소속, ORCID, 프로필 sameAs.

## 틀렸던 것

- **CV 를 안 열고 서지 DB 만 뒤졌다.** 소속을 Kim 그룹 단독으로 적고, 주소를 MIT 대표 주소로
  넣고, 참여 과제를 "데이터에 없다"고 보고했다. 셋 다 CV 1·5쪽에 있었다. user 지적.
  → memory `reference_cv_location_and_ownership` 등재.
- **기존 메모리를 어겼다.** Englund 표기 규칙이 이미 있었는데 CV 문구를 그대로 옮겼다.
  → 같은 메모리에 공개물 위계(Kim 먼저·단독, Englund 는 범위 한정) 추가.
- **공식 과제 제목을 임의로 줄였다.** MISTI 제목에서 `advanced quantum photonic integrated
  circuits` 를 잘랐고, 그게 Imperial 협업의 핵심이었다. 최종 보고서 기준으로 복원.
- **about 을 줄이자고 했다.** 중복만 보고 읽히는 자리를 안 봤다. user: 대부분 첫 화면만 본다.
- **CSS 명시도.** 번호 뱃지가 흰색으로 남은 것을 `!important` 만 붙여 두 번 놓쳤다.
- **빌드 확인.** `gh run watch` 종료 코드 0 인데 run 은 실패였고, 푸시 직후 `--limit 1` 로
  직전 빌드를 내 것으로 착각했다. 커밋 해시로 골라 기다리는 방식으로 바꿈.

## user 가 정한 것

- 페이지 역할: about 이 임팩트, research 가 깊이. 길이가 아니라 깊이로 구분.
- Sub-1-volt 는 2025 로 표기 (Crossref·CV 는 2026, 스크립트에 예외로 기록).
- cv 페이지의 research 중복은 알고도 그대로 둔다.
- 저널 뱃지 색 제거, 번호로. 링크색은 마젠타 → 초록(활동 그래프와 통일).
- 공저자 링크 제거. 한국어 이름은 본문에서 빼고 구조화 데이터에만.

## 남은 것 (user 손으로만)

GitHub 프로필 Website 칸, Google Scholar Homepage 칸, LinkedIn Contact info,
Google Search Console 등록. 랩 멤버 페이지 링크가 효과가 가장 크다.

## 오후: 공개 저장소 3건

사이트가 안정된 뒤, 공개할 만한 코드를 골라 세 저장소로 올렸다. 기준은 데이터를 빼고
아키텍처만 남겼을 때도 남이 쓸 수 있는가, 그리고 CMOS 집적 광학이 드러나는가였다.

- `yjinyoo/beol-eo-prescreen` — 전압 분배 / 경사 전극 쐐기장 / 1차 섭동.
  `prescreen_templates/` 118개 중 실제 재사용 기록이 있는 둘만 골랐다.
- `yjinyoo/refcheck` — 참고문헌을 세 색인에 대조. 출처(`Imbad0202/academic-research-skills`,
  CC BY-NC)를 확인했는데 상대는 색인 API 를 직접 부르지 않아 코드 겹침이 없다. MIT 로 냈다.
- `yjinyoo/harness-budget` — 컨텍스트 예산, 메모리 무결성, 죽은 경로, recall@K.

### 공개 준비가 찾아낸 결함

다듬는 과정 자체가 검사였다. 내부본에 남아 있는 것 셋:

1. refcheck 저널명 정규식이 문장을 삼켜 Nature 양식이 전부 "제목 없음"이 된다
2. refcheck 제목 추출이 `et al.` 에만 반응해 **BCS 논문이 NOT FOUND 로 나왔다**
3. link_lint 가 폴더명을 문자열로 박아 뒀다

1과 2는 Science 양식 교정지에서만 써서 안 드러났던 것이다. 공개본에서 고쳤고,
실제 교정지로 검증한 뒤 내부본에 이관해야 한다.

### 안 올린 것과 이유

- `leak_check.py` — 패턴 목록이 곧 협업자 이름과 프로젝트 ID라 user 가 제외를 지시
- `farfield_multipole.py`, `chiral_tmatrix.py` — 카이랄 원고 두 건이 미발표. 방법을 먼저 내주게 된다
- `fab_check.py` 의 `fab_constraints.md` — 29항목 중 47곳이 `internal` 출처이고
  진행 중 공정 경로와 미공개 원고의 판단 근거가 들어 있다. 엔진만 낼 것

---

## 오후 2부: 공개 저장소 둘 추가, tools 페이지, 사이트 자동 점검

### 올린 것

- **`yjinyoo/fab-check`** — 설계값을 공정 제약에 대조. 엔진은 내부본 그대로고 데이터 파일만
  갈았다(내부 제약 파일은 707줄에 내부 출처 47곳, 협업자 실명, 테이프아웃 스펙). 코드에서 뺀 것은
  내부 단계 이름과, self-test 가 우리 재료 목록을 코드에 박아두고 있던 것. 후자는 제약 파일이
  자기가 필요한 키를 `<!-- selftest-keys: ... -->` 로 선언하는 방식으로 바꿨다. 지문이 빠질 뿐
  아니라 검사가 나아진다(필요한 키 목록이 데이터 옆에 있다).
- **`yjinyoo/device-assert`** — 빌드된 시뮬레이션에서 소자를 직접 측정. 측정부 200줄은 그대로
  떼어냈고, 소자 스키마가 박혀 있던 판정부 330줄(프로젝트 언급 108곳)은 선언형 명세로 새로 썼다.
  시뮬레이터 없이 돌아가는 합성 시험을 넣었고, **틀린 소자 넷이 각각 틀린 줄에서 떨어지는지**를
  본다(통과만 보는 시험은 `True` 만 돌려주는 함수도 만족시킨다).
- **`harness-budget` 에 `latency_check.py` 추가** — 도구 시간과 모델 왕복 시간 분리.

### 안 올린 것

- 그림 도구 일곱 개 전부 (user 지시). `house_check` 계열은 BLOCK 0 이었지만 같이 뺐다.
- 본딩 사진 → GDS (OCT 프로젝트). **코드에서 치수와 시료 번호를 다 빼도 작업 방식 자체가 남고,
  그게 미발표 프로젝트의 방법이다.** user 판단: 애매하면 올리지 않는다. 일반화한 판본이
  `~/bondmap/bondmap.py` 에 로컬로만 있다(설정 파일화, 축척 오차를 출력으로, 합성 자체시험).
- Origin MCP — **우리 것이 아니다.** `youngminsw/Origin-Pro-MCP`, MIT, 별 39개, PyPI 등재.
  우리 체크아웃은 클론이고 `yjinyoo/origin-pro-mcp` 는 remote 이름부터 `backup`. 141 커밋 중
  124개가 원작자 것. **우리 17 커밋이 upstream 에 안 올라가 있다** → PR 대상.

### tools 페이지

처음엔 손으로 쓴 마크다운이었다. user 지적: 생성물 사이트에 혼자 손으로 쓰는 페이지고, 항목이
늘면 훑을 수가 없다. → `_data/tools.yml` + `scripts/update_tools.py` 로 바꿨다. 깃헙이 아는 것
(설명·언어·마지막 푸시)은 스크립트가 API 에서 긁고, 모르는 것(왜 만들었나)만 손으로 쓴다.
맨 위에 한 줄 목록이 붙는다. about 에서 링크한다.

### 자동 점검 (`site-maintenance.yml`, 매주 월요일)

- `tools` 잡: tools 데이터 갱신 → 바뀌면 커밋 + 재배포. **스스로 낫는다.**
- `checks` 잡: 내부 링크와 DOI 45개. 이건 사람이 고쳐야 하므로 실패로 알린다.
- 수동 실행으로 두 잡 다 통과 확인 (run 35478088194).

### user 교정 셋

1. **공개 코드에 한국어를 넣지 말 것.** 문구만이 아니라 토크나이저가 `[A-Za-z0-9가-힣]+` 였다.
   빠진 문자는 토큰이 아예 안 만들어져서 **결과에 "없음"으로도 안 나타난다** — 점수만 조용히
   떨어진다. 유니코드 단어 문자로 바꿨고, 어휘 제외 목록만 영어 기본값 + 옆에 `stopwords.txt`.
   구조만 남기고 언어는 쓰는 사람 몫.
2. **군더더기.** `Most of what runs the loop is project code and stays private` 같은 문장은
   읽는 사람에게 아무것도 안 주면서 비공개 코드가 있다는 것만 알린다.
3. **설명 없는 전문 용어.** `recall@K`. 용어를 빼고 하는 일로 썼다.

→ README 다섯 개를 같은 기준으로 훑었다. **지난 세션에 쓴 둘(471·537단어)에는 결함이 없고,
오늘 쓴 셋(860·1005·823)에만 있었다.** 뺀 것은 "다음 문장이 왜 중요한지 미리 알리는 문장"
다섯 개, 같은 말 반복 한 문단, 설명 없는 용어 하나. 길이가 아니라 **읽는 사람을 안내하려 든
자리**가 전부 군더더기였다.

### 내부본에 이관할 것 (3건 추가, 총 5건)

`Simulations/harness/known_issues.md` 2026-09-19 절에 등재. 층 두께가 샘플 간격만큼 짧게 나오는
버그, 누출 검사기 오탐 둘. 기존 3건(참고문헌 도구 둘, link_lint 폴더명)과 합쳐 5건.

---

## 2026-09-20 (같은 세션 이어서): 저장소 둘 추가, 페이지 정리, CV 원본 수정

### 올린 것

- **`yjinyoo/resonance-extract`** — 시간 영역 기록에서 극점을 맞춰 공진 주파수와 Q. 누출 검사
  두 파일 다 경고 0. 테스트 세 번 돌려 세 번 통과.
- **`yjinyoo/mpi-env-check`** — 병렬 작업 환경 게이트. 문서에 실제로 당한 네 건이 들어 있다.

**왜 처음에 빠졌나:** 인계의 "다음 공개 후보" 목록 두 개만 보고 그게 전부라고 여겼다.
user 가 "resonance extract 는 왜 안 올렸냐"고 물어서 `tools/` 를 직접 훑으니 둘이 더 나왔다.
**오늘 두 번째로 같은 실수다** (오전에는 현미경 이미지 건을 `head -20` 으로 자른 검색 결과에서
찾고 없다고 보고했다). 규칙: 목록을 믿지 말고 폴더를 본다.

### 페이지

- **publications 설명 제거.** 처음엔 "읽는 법"을 설명하고 있어서 내용을 바꿨는데, user 가
  "그것도 오버, 남들 페이지엔 이런 게 없다"고 해서 통째로 뺐다. **줄이는 것만 보고 없애는
  선택지를 안 봤다.**
- **cv 를 Fellowships / Awards 로 분리.** 일곱 줄이 같은 무게라 국가 경쟁 펠로우십이 학회
  포스터상과 나란히 있었다.
- **정부 장학금 줄에서 사고.** 절 머리글에 "NRF 가 개인에게 경쟁으로 수여"라고 썼는데 그 줄은
  GIST 재학에 따라오는 것이었다. 고칠 대상은 그 **문장**이었는데 내가 **줄**을 지웠다.
  user 교정: "그냥 이 말을 빼면 되는 거 아냐?" 내 판단(전원 지급 항목이 위의 둘을 깎는다)을
  오류 수정에 얹어서 실행한 것이고, 그건 따로 물었어야 했다. 줄은 복구.

### CV 원본 (해결)

docx 의 GIST 포닥 종료일 `Feb. 2022` → `Feb. 2023`. python-docx 로 run 하나만 교체(마지막
`22` 가 독립 run 이라 서식 손상 없음). **Word COM 이 이번에는 변환에 성공**해서 9월판 PDF 를
새로 뽑았고, 빌드의 `CORRECTIONS` 를 비웠다. 라이브 PDF 11쪽, 옛 날짜와 전화번호 모두 없음.

### 사이트 자동 점검

`site-maintenance.yml` 주간. `tools` 잡은 스스로 낫고(갱신 → 커밋 → 재배포), `checks` 잡
(링크 + DOI)은 사람이 고쳐야 하므로 실패로 알린다. 수동 실행으로 두 잡 다 통과.
첫 푸시가 일일 활동 그래프 워크플로와 부딪혀 거부됐다. 파일이 안 겹쳐 rebase 로 올렸다.
**자동화가 사람 작업과 부딪히는 건 앞으로도 난다.**

### 테마·서체

al-folio 유지, 서체도 유지(user 판단). 차별화가 필요하면 테마 교체보다 공정 도면 한 장이
먼저라는 데 합의. 도면 명세는 아직 안 썼다.

### CMT 공용 모듈 (새 세션으로 넘김)

결합 효율·부하 Q 를 18곳이, 원거리장 수집을 168곳이 각자 계산한다. 공용 모듈 없음.
명세 = `Simulations/harness/cmt_coupling_module.md`, 진입점 = 루트 인계.
**입력이 FDTD 원거리장이라 입력 규약이 명세의 절반**(정규화, 편광 벡터 중첩, 입체각 야코비안).
